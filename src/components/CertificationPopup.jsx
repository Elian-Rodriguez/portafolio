import React from "react";
//import ReactMarkdown from 'react-markdown';
//import "./ProjectPopup.css";

function CertificationPopup({ certificate, onClose }) {
  return (
<React.Fragment>
    {console.log(certificate)}
</React.Fragment>
    );
}

export default CertificationPopup;